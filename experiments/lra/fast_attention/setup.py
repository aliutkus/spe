import setuptools

setuptools.setup(
    name="fast-self-attention",
    author="Google",
    description="Fast Attention Via positive Orthogonal Random features",
    packages=setuptools.find_packages(),
)
